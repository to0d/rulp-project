(to-string "abc")
;=>"abc"

(to-string 123)
;=>"123"

(to-string a)
;=>"a"

(to-string '(a b c))
;=>"'(a b c)"

(to-string {a,b,c})
;=>"{a,b,c}"

(to-string (to-blob "abc"))
;=>"[616263]"

(to-string)
;err

(to-string a b)
;err
